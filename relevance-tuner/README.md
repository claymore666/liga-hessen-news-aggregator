# Liga Hessen Relevance Tuner

Embedding classifier and LLM analysis pipeline for news relevance classification for the Liga der Freien Wohlfahrtspflege Hessen.

## Architecture

```
News Item → Duplicate Check (paraphrase-mpnet, threshold 0.75)
                 ↓
         [Not duplicate]
                 ↓
         Embedding Classifier (nomic-v2 + sklearn)
                 ↓
         ┌───────┴───────┐
    [Relevant]       [Irrelevant]
         ↓                ↓
    LLM Analysis       Archive
    (qwen3:14b)
         ↓
    Store with priority/AK/summary
```

### Components

| Component | Model | Purpose |
|-----------|-------|---------|
| Embedding Classifier | nomic-v2 + RandomForest | Relevance, priority, AK classification |
| Duplicate Detection | paraphrase-multilingual-mpnet-base-v2 | Same-story detection (768d) |
| LLM Analysis | qwen3:14b-q8_0 (Ollama) | Summary, detailed analysis, tags |

## Documentation

| Document | Purpose |
|----------|---------|
| [docs/TRAINING_GUIDE.md](docs/TRAINING_GUIDE.md) | **Complete training guide** — export, train, deploy, rollback |
| [LABELING_PROMPT.md](LABELING_PROMPT.md) | Labeling criteria for the LLM (German) |
| [CLAUDE.md](CLAUDE.md) | Claude Code project instructions |

## Quick Start: Retrain Classifier

```bash
cd /home/kamienc/claude.ai/ligahessen/relevance-tuner
source venv/bin/activate

# 1. Export training data from production (via SSH tunnel)
ssh -L 9000:localhost:8000 docker-ai -N -f
API_URL=http://localhost:9000/api python scripts/export_training_data.py --min-content-length 200
pkill -f "ssh -L 9000:localhost:8000"

# 2. Train (MUST set EMBEDDING_BACKEND!)
EMBEDDING_BACKEND=nomic-v2 python train_embedding_classifier.py

# 3. Deploy to classifier container
docker cp models/embedding/embedding_classifier_nomic-v2.pkl \
  liga-classifier:/app/models/embedding_classifier_nomic-v2.pkl

# 4. Verify
curl -s http://localhost:8082/health | jq
```

See [docs/TRAINING_GUIDE.md](docs/TRAINING_GUIDE.md) for the full step-by-step guide.

## Project Structure

```
relevance-tuner/
├── data/final/                  # Training splits (generated by export)
│   ├── train.jsonl              # 70% training
│   ├── validation.jsonl         # 15% validation
│   └── test.jsonl               # 15% test
├── models/embedding/            # Trained classifier output
│   ├── embedding_classifier_nomic-v2.pkl
│   └── metrics.json             # Training history
├── scripts/
│   ├── export_training_data.py  # Export from production DB
│   ├── label_with_ollama.py     # Label with Ollama LLM
│   ├── create_splits.py         # Create stratified splits
│   ├── sync_vectordb.py         # Sync vector store with DB
│   └── compare_classifier_vs_llm.py  # Model comparison
├── services/classifier-api/     # FastAPI classifier service (Docker)
│   ├── classifier.py            # EmbeddingClassifier + stores
│   ├── main.py                  # API endpoints
│   └── models/                  # Deployed model (volume-mounted)
├── utils/
│   ├── embeddings.py            # Embedding backends
│   └── data_loading.py          # Data loading utilities
├── train_embedding_classifier.py  # Main training script
├── config.py                    # Hyperparameters, AK/priority definitions
└── docs/TRAINING_GUIDE.md       # Complete training documentation
```

## Classifier API

Runs on port 8082 (gpu1). GPU-accelerated embedding classification.

| Endpoint | Method | Purpose |
|----------|--------|---------|
| `/health` | GET | Health check with index stats |
| `/classify` | POST | Classify article (relevance, priority, AKs) |
| `/search` | POST | Semantic search |
| `/find-duplicates` | POST | Find similar articles |
| `/index` | POST | Index single item |
| `/index/batch` | POST | Batch index items |
| `/ids` | GET | List indexed item IDs |
| `/delete` | POST | Delete items from indexes |
| `/sync-duplicate-store` | POST | Sync duplicate store from search store |

## Last Training Metrics (nomic-v2, Jan 20 2026)

| Metric | Value |
|--------|-------|
| Training items | 3,519 |
| Relevance accuracy | 89.6% |
| Priority accuracy | 56.1% |
| Priority within-1-level | 95.9% |
| AK accuracy | 69.9% |
| Speed | 126.8 items/sec |

## Requirements

- Python 3.11+
- CUDA GPU (RTX 3090 recommended, optional for training)
- Docker & Docker Compose
- Access to production database (docker-ai) for training data export
